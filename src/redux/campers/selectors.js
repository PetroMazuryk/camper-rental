export const selectCampersData = state => state.campers.items;

export const selectIsCampersLoading = state => state.campers.isLoading;

export const selectCampersErroring = state => state.campers.error;

export const selectAllCampers = state => state.campers;
